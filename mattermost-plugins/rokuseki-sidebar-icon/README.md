# Rokuseki Sidebar Icon Plugin

Optional experimental Mattermost webapp plugin that replaces the sidebar icon
for the `triad-lab` channel with the Rokuseki crest.

This plugin is intentionally kept separate from `jp.sunwood.rokuseki-brand` so
the main branding plugin can stay lightweight. Keep this one disabled unless
you explicitly want to test sidebar icon replacement.
